{
  "name": "channel-talk-inbound",
  "version": "0.1.0",
  "private": true,
  "description": "T2 Channel Talk Inbound Worker — URL token 검증 + normalize + Supabase INSERT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.4",
    "wrangler": "^3.80.0"
  },
  "engines": {
    "node": ">=20"
  }
}
